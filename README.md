
  # Build Stock Management Backend

  This is a code bundle for Build Stock Management Backend. The original project is available at https://www.figma.com/design/HhM0NpFtPL2SVaWrQvFy5D/Build-Stock-Management-Backend.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  